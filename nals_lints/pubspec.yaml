name: nals_lints
description: A new Flutter package project.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.5.4 <4.0.0"

dependency_overrides:
  analyzer: 5.13.0

dependencies:
  custom_lint_builder: ^0.7.0
  source_gen: ^2.0.0
